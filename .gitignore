node_modules/
.next/
.vercel/
.env*
archives/
*.tsbuildinfo
.build/
Package.resolved
xcuserdata/
*.xcuserstate

# Android signing credentials must never be committed.
*.jks
*.keystore
keystore.properties

# Locally generated signed Android release artifacts.
android/WindBrassTuner/app/release/

# Keep raw App Store device captures local; commit only final delivery assets.
ios/StringTuner/AppStore/v1.1/source/
