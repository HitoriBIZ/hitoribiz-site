import { mockCampaigns } from "../../../../lib/newsletter/mockData";
import { formatDate, PageHeader, StatusBadge } from "../../../components/newsletter/AdminUi";

export default function CampaignsPage() {
  return <div className="mx-auto max-w-7xl"><PageHeader title="キャンペーン" description="作成したメールマガジンと配信状況を管理します。" action={{ label: "キャンペーンを作成", href: "/admin/newsletter/campaigns/new" }} /><div className="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-[950px] w-full text-left text-sm"><thead className="bg-slate-100 text-xs uppercase tracking-wide text-slate-600"><tr>{["キャンペーン", "ステータス", "対象タグ", "作成日", "配信予定日", "配信済み件数"].map((head) => <th key={head} className="px-4 py-3 font-semibold">{head}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{mockCampaigns.map((campaign) => <tr key={campaign.id} className="hover:bg-slate-50"><td className="px-4 py-4"><p className="font-semibold text-slate-900">{campaign.name}</p><p className="mt-1 text-xs text-slate-500">件名: {campaign.subject}</p></td><td className="px-4 py-4"><StatusBadge status={campaign.status} /></td><td className="px-4 py-4"><div className="flex flex-wrap gap-1">{campaign.targetTags.map((tag) => <span key={tag.id} className="rounded-full bg-blue-50 px-2 py-1 text-xs text-blue-700">{tag.name}</span>)}</div></td><td className="whitespace-nowrap px-4 py-4 text-slate-600">{formatDate(campaign.createdAt)}</td><td className="whitespace-nowrap px-4 py-4 text-slate-600">{formatDate(campaign.scheduledAt, true)}</td><td className="px-4 py-4 font-semibold text-slate-800">{campaign.sentCount.toLocaleString("ja-JP")}</td></tr>)}</tbody></table></div></div><p className="mt-4 text-xs text-slate-500">Phase 1では一覧表示のみです。保存、編集、テスト送信、本配信は実行されません。</p></div>;
}
