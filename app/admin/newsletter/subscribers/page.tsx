import { mockSubscribers } from "../../../../lib/newsletter/mockData";
import { formatDate, PageHeader, StatusBadge } from "../../../components/newsletter/AdminUi";

export default function SubscribersPage() {
  return <div className="mx-auto max-w-7xl"><PageHeader title="読者一覧" description="購読状態、関心テーマ、同意情報を確認します。" /><div className="mt-6 overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-[1100px] w-full text-left text-sm"><thead className="bg-slate-100 text-xs uppercase tracking-wide text-slate-600"><tr>{["読者", "会社名・屋号", "関心テーマ", "タグ", "ステータス", "登録日", "同意日時・経路"].map((head) => <th key={head} className="px-4 py-3 font-semibold">{head}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{mockSubscribers.map((sub) => <tr key={sub.id} className="align-top hover:bg-slate-50"><td className="px-4 py-4"><p className="font-semibold text-slate-900">{sub.name || "氏名未登録"}</p><p className="mt-1 text-xs text-slate-500">{sub.email}</p></td><td className="px-4 py-4 text-slate-700">{sub.companyName || "—"}</td><td className="max-w-56 px-4 py-4 text-xs leading-5 text-slate-600">{sub.interests.join(" / ") || "—"}</td><td className="px-4 py-4"><div className="flex flex-wrap gap-1">{sub.tags.length ? sub.tags.map((tag) => <span key={tag.id} className="rounded-full bg-blue-50 px-2 py-1 text-xs text-blue-700">{tag.name}</span>) : "—"}</div></td><td className="px-4 py-4"><StatusBadge status={sub.status} /></td><td className="whitespace-nowrap px-4 py-4 text-slate-600">{formatDate(sub.createdAt)}</td><td className="whitespace-nowrap px-4 py-4 text-xs text-slate-600">{formatDate(sub.consentAt, true)}<br /><span className="text-slate-400">{sub.consentSource}</span></td></tr>)}</tbody></table></div></div><p className="mt-4 text-xs text-slate-500">Phase 1では閲覧のみです。読者追加・編集・CSVエクスポートはPhase 2で実装します。</p></div>;
}
